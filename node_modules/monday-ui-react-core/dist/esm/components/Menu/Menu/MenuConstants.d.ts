import { ReactElement } from "react";
export declare type CloseMenuOption = {
    propagate?: boolean;
};
export declare type MenuChild = ReactElement & {
    type?: {
        isSelectable?: boolean;
        isMenuChild?: boolean;
        isMenu?: boolean;
        supportFocusOnMount?: boolean;
    };
};
